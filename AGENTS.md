# What is Spindle?

Spindle is a single-binary engine that unifies queues, streams, object change events, and durable workflows into one execution model. It provides the controls needed to run them reliably at scale, including concurrency management, rate limits, and durable coordination.
